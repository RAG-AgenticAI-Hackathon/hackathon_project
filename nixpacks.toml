[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = ["pip install -r backend/requirements.txt"]

[phases.build]
cmds = ["cd backend && python ingest.py"]

[start]
cmd = "cd backend && uvicorn api:app --host 0.0.0.0 --port $PORT"