# PDF Extraction 
pdfplumber==0.10.3          # Extract text + tables from PDFs

# Embeddings 
openai==1.30.1              # text-embedding-3-small (embeddings only)
tiktoken==0.7.0             # Count tokens (to verify chunk sizes)

# Vector Store
chromadb==0.5.0             # Local vector database

# LLM
anthropic==0.25.8           # Claude API (claude-3-5-sonnet)

# Backend API 
fastapi==0.111.0            # REST API framework
uvicorn==0.29.0             # ASGI server to run FastAPI
python-dotenv==1.0.1        # Load .env file
pydantic==2.7.1             # Request/response validation

# Utilities
python-multipart==0.0.9     # For file uploads (if needed)
httpx==0.27.0               # Async HTTP (used internally by FastAPI)
loguru==0.7.2               # Better logging than print()